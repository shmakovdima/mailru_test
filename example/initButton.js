(function (window) {
  'use strict';

  var buttonParams = {
    cssSels: {
      Main: {
        textContainer: '.text-container'
      }
    }
  };

  window.ru.mail.cpf.modules.Button(buttonParams, null, $('#button-container'));

})(window);
