(function (window) {
  'use strict'

  // Используем синтаксис ES5, так как в constructors.js есть ссылки на полифилы es5

  var dropdownParams = {
    cssSels: {
      modificators: {
        activeClass: 'dropdown__item__active', // класс активного элемента
        disabledClass: 'dropdown__item__disabled' // класс disabled элемента
      }
    }
  }


})(window);
